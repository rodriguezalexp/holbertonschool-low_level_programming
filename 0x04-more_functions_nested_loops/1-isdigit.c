/**
 * _isdigit - checks for a digit
 * @c: is the argument of the protoype
 * Return: 1 if is c, 0 oyherwise
 */

int _isdigit(int c)

{
	int i;

	for (i = 0; i < 10; i++)
	{
		c = i;
		if (c == i)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
