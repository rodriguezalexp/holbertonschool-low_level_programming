/**
 * _isupper - checks if passes character is uppercase
 * @c: is the argument of the protoype
 * Return: 1 if is c, 0 oyherwise
 */

int _isupper(int c)

{
if (c >= 'A' && c <= 'Z')
	return (1);
else
	return (0);

}
